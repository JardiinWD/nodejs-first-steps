// Declaration of a constant named amount with a numerical value
const amount = 9;

// Check if the value of amount is less than 10
if (amount < 10) {
  // If true, log 'small number' to the console
  console.log('small number');
} else {
  // If false, log 'large number' to the console
  console.log('large number');
}

// Log a message to the console indicating that it's the first Node.js app
console.log(`hey it's my first node app!!!`);
