// Defining a function 'sayHi' that takes a 'name' parameter
const sayHi = (name) => {
  // Logging a greeting message with the provided name to the console
  console.log(`Hello there ${name}`);
};

// Exporting the 'sayHi' function as the default export for the module
module.exports = sayHi;
