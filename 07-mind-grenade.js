// Declaration of constants num1 and num2 with numerical values
const num1 = 5;
const num2 = 10;

// Definition of a function named addValues
function addValues() {
  // Console log the sum of the two constants
  console.log(`the sum is : ${num1 + num2}`);
}

// Call the addValues function to execute the calculation and display the result
addValues();
